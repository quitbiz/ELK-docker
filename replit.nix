{ pkgs }: {
    deps = [
        pkgs.sudo
        pkgs.docker-compose
        pkgs.docker-compose
        pkgs.docker-compose
        pkgs.docker-compose
        pkgs.docker-compose
        pkgs.bashInteractive
        pkgs.man
    ];
}